#
# mcp server configuration
# cf. https://github.com/microsoft/playwright-mcp
#
mcpServers:
  playwright:
    transport: stdio
    command: docker
    args:
      - run
      - -i
      - --rm
      - mcp/playwright
      - --headless
      - --isolated
      - --no-sandbox
      - --browser
      - chromium
      - --output-dir
      - /tmp
    env:
      DOCKER_MCP_IN_CONTAINER: "1"
      DOCKER_MCP_IN_DIND: "1"
